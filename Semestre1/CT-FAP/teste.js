array = [1, 2, 3, 4, 5, 6, 6, 7];
nomes = Array(2);
array.shift(); // tira a ultima informação

matriz = [["Pikachu", "m", "lvl01"],
["charmander" , "m", "lvl04"]];
console.log(`o pokemon "${matriz[0][0]}" é o sexo ${matriz[1][1]} e está no nível ${matriz[1][2]}`);

a = 8 
b = 9

if (a>b && a==8 || a== 8 && b == 9) {
    console.log ("até q deu certo")
}else {
    console.log ("deu ruim msm")
}

aprovacao = false

if (!aprovacao) { // se o usuário NÃO for aprovado
   console.log("vc rodou, paizão"); 
}else {
    console.log("passou")
}

a += 100
console.log(a)